const responseArr = { responseCount: 1, responses: [] };

const Responses = () => {
  //응답이 오면 보여주기

  return (
    <div>
      <div>응답 {}</div>
    </div>
  );
};

export default Responses;

/**
 * 질문 title
 * 응답 갯수
 * 옵션에 대한 갯수
 *
 */
